# ✅ 班级名称修正和数据迁移

## 🎯 完成的工作

### 1. 修正下拉选项
已将 **2025级A乙6** 改为 **2024级A乙6** ✓

**当前班级选项：**
- 2025级A甲2
- 2025级A乙2
- 2024级A甲6
- 2024级A乙6 ← 已修正

---

## 📋 数据迁移任务

需要将数据库中的旧班级名称更新为新格式：

| 旧名称 | 新名称 |
|--------|--------|
| A甲2 | 2025级A甲2 |
| A乙2 | 2025级A乙2 |
| A甲6 | 2024级A甲6 |
| A乙6 | 2024级A乙6 |

---

## ⚡ 立即执行（5分钟）

### 步骤1：打开 Supabase SQL Editor

登录 [Supabase Dashboard](https://app.supabase.com)

### 步骤2：查看当前数据

```sql
-- 查看需要更新的班级
SELECT 
  class_name as 班级,
  COUNT(*) as 记录数,
  COUNT(DISTINCT student_name) as 学生数
FROM practice_records
WHERE class_name IN ('A甲2', 'A乙2', 'A甲6', 'A乙6')
GROUP BY class_name
ORDER BY class_name;
```

### 步骤3：备份数据（重要！）

```sql
-- 创建备份表
CREATE TABLE IF NOT EXISTS practice_records_backup_migration AS 
SELECT * FROM practice_records 
WHERE 1=0;

-- 备份数据
INSERT INTO practice_records_backup_migration
SELECT * FROM practice_records
WHERE class_name IN ('A甲2', 'A乙2', 'A甲6', 'A乙6');

-- 同样备份 students 表
CREATE TABLE IF NOT EXISTS students_backup_migration AS 
SELECT * FROM students 
WHERE 1=0;

INSERT INTO students_backup_migration
SELECT * FROM students
WHERE class_name IN ('A甲2', 'A乙2', 'A甲6', 'A乙6');
```

### 步骤4：执行迁移

```sql
-- 更新 practice_records 表
UPDATE practice_records
SET class_name = '2025级A甲2'
WHERE class_name = 'A甲2';

UPDATE practice_records
SET class_name = '2025级A乙2'
WHERE class_name = 'A乙2';

UPDATE practice_records
SET class_name = '2024级A甲6'
WHERE class_name = 'A甲6';

UPDATE practice_records
SET class_name = '2024级A乙6'
WHERE class_name = 'A乙6';

-- 更新 students 表
UPDATE students
SET class_name = '2025级A甲2'
WHERE class_name = 'A甲2';

UPDATE students
SET class_name = '2025级A乙2'
WHERE class_name = 'A乙2';

UPDATE students
SET class_name = '2024级A甲6'
WHERE class_name = 'A甲6';

UPDATE students
SET class_name = '2024级A乙6'
WHERE class_name = 'A乙6';
```

### 步骤5：验证结果

```sql
-- 确认旧名称已不存在（应该返回空）
SELECT class_name, COUNT(*) 
FROM practice_records
WHERE class_name IN ('A甲2', 'A乙2', 'A甲6', 'A乙6')
GROUP BY class_name;

-- 查看新名称的记录数
SELECT 
  class_name as 班级,
  COUNT(*) as 记录数,
  COUNT(DISTINCT student_name) as 学生数
FROM practice_records
WHERE class_name IN ('2025级A甲2', '2025级A乙2', '2024级A甲6', '2024级A乙6')
GROUP BY class_name
ORDER BY class_name;

-- 查看班级统计
SELECT * FROM class_stats ORDER BY class_name;
```

---

## 📁 完整脚本

我已经为您准备了完整的迁移脚本：**`migrate_class_names.sql`**

包含：
- ✅ 数据查看
- ✅ 备份步骤
- ✅ 迁移执行
- ✅ 结果验证
- ✅ 回滚方案

直接打开该文件，按顺序执行即可。

---

## 🎯 迁移效果

### 迁移前

```
教师后台 - 班级列表：
├─ A甲2      (X人)
├─ A乙2      (X人)
├─ A甲6      (X人)
└─ A乙6      (X人)
```

### 迁移后

```
教师后台 - 班级列表：
├─ 2025级A甲2  (X人)  ✓
├─ 2025级A乙2  (X人)  ✓
├─ 2024级A甲6  (X人)  ✓
└─ 2024级A乙6  (X人)  ✓
```

---

## 🔄 部署流程

### 1. 执行数据库迁移

按照上面的步骤执行 SQL 脚本

### 2. 部署前端代码

```bash
npm run build  # ✅ 已验证成功
# 部署到生产环境
```

### 3. 刷新教师后台

打开教师后台，点击"刷新"按钮，验证班级名称已更新。

### 4. 通知学生

告知学生：
- 新注册的学生从下拉框选择班级
- 已注册的学生信息会自动更新

---

## ⚠️ 注意事项

### 1. 备份优先

**务必先执行备份步骤！** 虽然更新操作很安全，但备份是最佳实践。

### 2. 验证结果

执行后一定要运行验证 SQL，确认：
- 旧名称已不存在
- 新名称记录数正确
- 学生数没有丢失

### 3. 保留备份表

建议保留备份表 1-2 周，确认没问题后再删除：

```sql
-- 确认没问题后删除备份
DROP TABLE IF EXISTS practice_records_backup_migration;
DROP TABLE IF EXISTS students_backup_migration;
```

---

## 🔧 如果需要回滚

如果发现问题，可以从备份恢复：

```sql
-- 从备份恢复
UPDATE practice_records pr
SET class_name = prb.class_name
FROM practice_records_backup_migration prb
WHERE pr.id = prb.id;

UPDATE students s
SET class_name = sb.class_name
FROM students_backup_migration sb
WHERE s.id = sb.id;
```

---

## ✅ 检查清单

数据库迁移：
- [ ] 查看当前数据
- [ ] 创建备份表
- [ ] 执行迁移 SQL
- [ ] 验证结果
- [ ] 查看班级统计

前端部署：
- [ ] 构建成功（已完成 ✓）
- [ ] 部署到生产环境
- [ ] 测试下拉框显示正确
- [ ] 验证新学生注册

教师后台：
- [ ] 刷新数据
- [ ] 检查班级列表
- [ ] 验证统计准确
- [ ] 测试筛选功能

---

## 🎉 完成后效果

1. ✅ **下拉框正确** - 显示 2024级A乙6（不是2025级）
2. ✅ **数据统一** - 所有旧数据已更新为新格式
3. ✅ **统计准确** - 教师后台显示带年级的班级名称
4. ✅ **向前兼容** - 新老数据无缝衔接

---

**现在就执行迁移脚本，5分钟完成数据更新！** 🚀

---

**创建时间：** 2024-04-29  
**修改文件：** 
- `src/components/StudentInfo.tsx` (已修正 ✓)
- `migrate_class_names.sql` (迁移脚本 ✓)

**构建状态：** ✓ npm run build 成功  
**待执行：** 数据库迁移 SQL
